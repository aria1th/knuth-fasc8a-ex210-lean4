import KnuthFasc8AEx210.Closed.TrelResidualCertificate

/-!
# Heavy closed-certificate target

This root imports all generated `Trel+` row-block residual certificates and the
semantic coverage wrapper. It is built explicitly in CI but is not part of the
ordinary default target.
-/
