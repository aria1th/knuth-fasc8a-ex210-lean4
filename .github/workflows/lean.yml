name: Lean 4

on:
  pull_request:
    branches: ["main"]
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref || github.ref_name }}
      - name: Export canonical Lean certificate data
        run: python3 scripts/export_lean_certificate_hex.py
      - name: Commit generated certificate data
        if: github.event_name == 'pull_request'
        shell: bash
        run: |
          if [ -z "$(git status --porcelain -- data/lean)" ]; then
            echo "Generated certificate data is already current."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/lean
          git commit -m "pr3: regenerate Lean certificate hex data"
          git push origin HEAD:${{ github.head_ref }}
      - name: Upload generated certificate data
        uses: actions/upload-artifact@v4
        with:
          name: lean-certificate-hex
          path: data/lean
          if-no-files-found: error
          retention-days: 7
      - name: Set up Lean and restore the mathlib cache
        uses: leanprover/lean-action@v1
        continue-on-error: true
        with:
          auto-config: false
          build: true
          test: false
          lint: false
          use-mathlib-cache: true
      - name: Build the Lean library
        shell: bash
        run: |
          set +e
          lake build > lean-build.log 2>&1
          status=$?
          if [ "$status" -ne 0 ]; then
            grep -E -B 3 -A 20 '(^| )error:|^✖|Lean exited|build failed' lean-build.log \
              | tail -n 300 > lean-errors.log
            cat lean-errors.log
          fi
          exit "$status"
      - name: Upload failed Lean build logs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: lean-build-log
          path: |
            lean-build.log
            lean-errors.log
          if-no-files-found: warn
          retention-days: 7
      - name: Post failed-build diagnostics to the pull request
        if: failure() && github.event_name == 'pull_request'
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          {
            echo '<!-- lean-ci-diagnostics -->'
            echo '### Lean build diagnostics'
            echo '```text'
            cat lean-errors.log
            echo '```'
          } > lean-diagnostics.md
          gh pr comment "$PR_NUMBER" --body-file lean-diagnostics.md
      - name: Reject proof placeholders and local axioms
        shell: bash
        run: |
          if grep -RInE --include='*.lean' '(^|[[:space:]])(sorry|admit|axiom)([[:space:]]|$)' \
              KnuthFasc8AEx210.lean KnuthFasc8AEx210; then
            echo "Lean proof placeholder or local axiom found"
            exit 1
          fi
