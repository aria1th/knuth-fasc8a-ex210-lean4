import KnuthFasc8AEx210.Closed.EmbeddedTrel

/-!
# Heavy closed-certificate checks

This module imports checks that execute large embedded payloads, such as the
full `Trel_plus.kmc` residual computation. It is intentionally not imported by
`KnuthFasc8AEx210.Closed` so the default CI build remains reviewable.
-/
